using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sxxianshi : MonoBehaviour
{
    private GameObject nltext;//ƒ‹¡¶œ‘ æ
    // Start is called before the first frame update
    void Start()
    {
        //nltext=transform.GetChild(0).gameObject;
        //nltext.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
       
    }
    public void xianshi()
    {
        //nltext.SetActive(true);
    }
    public void yingcang()
    {
        //nltext.SetActive(false);
    }
}
